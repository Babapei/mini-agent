## 题目：应用运行日志智能分析

某桌面端或服务端应用运行过程中会持续产生大量日志。随着用户量和系统复杂度增加，开发人员通过人工搜索日志定位错误、性能问题和请求链路的效率越来越低。

请实现一个**智能日志分析工具**，能够读取应用日志，对错误、性能问题以及请求调用链进行分析，并生成一份便于开发人员阅读的问题分析报告。

### 基础功能要求

系统至少需要完成以下能力：

1. 读取一个或多个日志文件。
2. 识别并统计不同日志级别，例如：
   - INFO
   - WARN
   - ERROR
3. 按模块统计：
   - 日志数量；
   - ERROR 数量；
   - ERROR 比例。
4. 分析请求耗时，并至少计算：
   - 平均耗时；
   - P95；
   - P99。
5. 支持输入 `traceId`，还原该请求对应的完整日志链路。
6. 汇总主要异常，并输出 Markdown、HTML 或其他便于阅读的分析报告。
7. 对错误日志、异常请求或性能问题提供一定程度的问题归纳。

### 日志示例

```text
2026-08-10 10:00:01.123 INFO  [UserService] [traceId=abc001] request start userId=10001
2026-08-10 10:00:01.231 INFO  [UserService] [traceId=abc001] load user success cost=108ms
2026-08-10 10:00:01.245 INFO  [OrderService] [traceId=abc001] query order cost=14ms
2026-08-10 10:00:01.260 INFO  [UserService] [traceId=abc001] request end totalCost=137ms

2026-08-10 10:00:02.100 INFO  [OrderService] [traceId=abc002] request start userId=10002
2026-08-10 10:00:02.800 WARN  [Database] [traceId=abc002] slow query cost=700ms
2026-08-10 10:00:03.500 ERROR [OrderService] [traceId=abc002] query order failed error=database_timeout
2026-08-10 10:00:03.510 ERROR [Gateway] [traceId=abc002] request failed status=500 totalCost=1410ms
```

### 测试材料生成

如果没有提供现成日志文件，候选人可以自行编写脚本或使用 AI 工具生成测试日志。

建议生成：

- 5,000～20,000 行日志；
- 5～10 个业务模块；
- 至少 100 个不同 traceId；
- 正常请求；
- 异常请求；
- 慢请求；
- timeout；
- database error；
- network error；
- malformed log；
- 部分字段缺失的日志。

要求测试数据不能全部为完全规则的数据，需要包含一定比例的异常格式和边界场景。

生成的数据不计入核心功能实现，但需要提交生成脚本或说明生成方法。

### 可选增强项

包括但不限于：

- 自动发现异常模式；
- 相似错误聚类；
- Top N 慢请求；
- 时间趋势分析；
- 图表展示；
- 增量日志分析；
- 使用 LLM 总结异常原因；
- 对可能的 Root Cause 给出候选分析；
- 针对日志格式变化提供一定容错能力；
- 对超大日志文件进行性能优化。

## 候选人提交要求-交付物

至少提交：

1. **完整源代码**。
2. **README**，至少包括：
   - 项目说明；
   - 环境要求；
   - 安装方式；
   - 启动方式；
   - 使用示例。
3. **测试日志或日志生成脚本**。
4. **至少一份由程序实际生成的分析报告**。
5. **测试代码或测试说明**。
6. **AI 工具完整对话过程**：
   - 导出本次考试期间与 Codex、Claude Code、Cursor、OpenCode 等 AI 工具的主要对话记录；
   - 应尽量保留从需求理解、方案设计、代码生成、调试、测试到最终修改的过程；
   - 若工具无法直接导出，可通过 Markdown、文本、截图或其他可查看形式提交。
7. **AI_USAGE.md**，简要总结：
   - 使用了哪些 AI 工具；
   - AI 主要帮助完成了哪些工作；
   - 哪些 AI 建议直接采用；
   - 哪些 AI 建议经过修改后采用；
   - 哪些 AI 建议被否决，以及原因。
8. **设计说明**，可单独提交 `DESIGN.md`，也可包含在 README 中，至少说明：
   - 整体架构；
   - 核心数据结构；
   - 日志分析流程；
   - 主要技术取舍；
   - 当前已知问题；
   - 如果继续完善，下一步准备改进什么。