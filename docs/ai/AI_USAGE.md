# AI 使用记录

本文件按阶段追加。不把实现结束后的印象倒填成当时没有发生的决定。

## 阶段 0

### AI 参与了哪些环节

- 阅读第四题，整理基础能力、工具、测试材料和交付物。
- 把候选人选定的 Python、Mock 与真实模型双后端，写成阶段计划、设计说明、决定记录和门禁。

### 哪些关键设计由候选人决定

- 先写详细计划书和阶段文档，再写代码。每阶段验收后再继续，避免后面越写越简陋。
- Agent 本体使用 Python。
- Mock 和真实模型都要交付可检查的结果，不能只做其中一个。

### AI 出现过哪些错误判断

- 第一版口头说明把「不要一次写完所有代码」和「一步一步做」放在一起，候选人追问后才能确认：执行方式是每阶段停止并等待查看，而不是一口气做完。
- 在候选人明确要求完成本计划的全部待办之前，AI 不应把八个阶段当成一次无人值守的连续实现。

### 候选人如何发现并修正

- 候选人连续追问「是不是一次性做完」「能不能一步一步来」「会不会不停做到完」。
- 据此把执行方式收成：阶段 0 只写文档；每个阶段有允许目录、验收命令和完成记录。
- 后续如果一次会话被要求完成全部门禁，仍然按阶段更新本文件和计划书，不跳过验收。

## 阶段 1 到阶段 4

### AI 参与了哪些环节

- 按已冻结的设计实现 workspace 生成、四个工具、Mock 决策和主循环。
- 为每层写了独立测试：材料快照、工具成败、Mock 下一步、循环里的多轮调用和停止条件。

### 哪些关键设计由候选人决定

- 阶段顺序和「答案必须来自工具结果」沿用阶段 0 已确认的计划，实现时没有改成把三个任务的答案写死。

### AI 出现过哪些错误判断

- 这个区间没有先改设计边界。测试在实现后一次跑通工具、Mock 和循环。

### 候选人如何发现并修正

- 门禁仍以 `pytest` 为准，不把未跑的测试写成已通过。

## 阶段 5 到阶段 7

### AI 参与了哪些环节

- 实现 OpenAI 兼容适配器、命令行、四个固定任务的检查脚本、README 和 Trace。
- 用响应夹具测试工具调用、最终答案、畸形响应、网络重试和 4xx 不重试。
- 检查环境变量。没有密钥，因此没有发起真实模型请求。

### 哪些关键设计由候选人决定

- Mock 和真实模型都要有可检查的结果。没有密钥时，真实结果的位置留空，不拿 Mock Trace 顶替。

### AI 出现过哪些错误判断

- `tests/test_openai_adapter.py` 里响应类的类型注解引用了自身，文件又没有 `from __future__ import annotations`。收集测试时出现 `NameError`，四个阶段的测试都没能开始跑。
- 任务脚本先用两个工具调用的 Trace 顺序检查全部任务。越界任务只会失败一次然后结束，套用这条顺序会把正确行为判成失败。写检查函数时就改成越界任务只要求一轮工具调用。
- 任务脚本在检查 OpenAI 环境变量之前就重新生成 workspace。缺少密钥的那次运行把 Mock 刚刚写出的 `todo-report.md`、`report.md` 和 `summary.md` 删掉了。

### 候选人如何发现并修正

- `NameError` 由 `python3 -m pytest` 的收集错误暴露，补上 future import 后重新运行。
- 越界任务的 Trace 形状对照的是「失败一次就停止」，不是「至少两个工具」。检查脚本按任务种类分开，随后 Mock 实跑通过。
- 缺少密钥的运行结束后，workspace 里的三份报告不见了，而 `docs/results/mock.md` 还写着通过。据此把生成材料挪到凭证检查之后，加了 `tests/test_run_tasks.py`，再重新跑 Mock 任务，报告和 Trace 一起留下。
- 全量结果：`python3 -m pytest` 为 26 passed。`python3 scripts/run_tasks.py --llm openai` 退出码 2，并说明缺少 `OPENAI_API_KEY`、`OPENAI_BASE_URL`、`OPENAI_MODEL`。
