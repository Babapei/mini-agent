[project]
name = "mini-agent"
version = "0.1.0"
description = "一个会连续调用工具的 Mini Agent"
requires-python = ">=3.11"
readme = "README.md"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
